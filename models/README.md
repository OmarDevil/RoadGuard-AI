# Model Files

Place trained model weights here.

- `vehicle_detector.pt`: optional custom traffic object detector.
- `helmet_detector.pt`: helmet/no-helmet detector with classes such as `helmet`, `no_helmet`, and `rider`.

The default configuration uses `yolo11n.pt` for general COCO traffic detection.

